# Copy to .env and fill in. For local dev the API runs with zero config:
# sessions live in memory and are lost on restart (documented limitation).
PORT=3001
WEB_ORIGIN=http://localhost:5173
# Set these when the Supabase project exists (Postgres store replaces memory).
# DATABASE_URL=postgresql://...
# SUPABASE_SERVICE_ROLE_KEY=...
